services:
  nginx:
    image: nginx:alpine
    container_name: nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ../confs/nginx.conf:/etc/nginx/nginx.conf:ro
      - ../certs:/etc/nginx/certs:ro
      - ../confs/domainNotFound.html:/usr/share/nginx/html/domainNotFound.html:ro
    networks:
      - api-gateway-env