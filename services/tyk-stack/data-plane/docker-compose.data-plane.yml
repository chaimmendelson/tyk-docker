services:
  # ---------------- Base services ----------------
  tyk-data-plane-gateway:
    image: tykio/tyk-gateway:${GATEWAY_VERSION:-v5.6.1}
    container_name: tyk-data-plane-gateway
    env_file:
      - tyk-stack/data-plane/confs/gateway.conf
    networks:
      - api-gateway-env
 